日志-2025/10/29 20：17
为首次日志，先总结当前进度。
首先完成了向量class类型的基本关键。完成了基础的构造函数与析构函数。
当前的向量仅支持int的数据类型，预计会拓展float的类型计算。计划在完成float类型的拓展后尝试使用类模板的形式去完成更多的数据类型。
同时关于运算符重载，以禁用了部分运算符的函数重载。对于赋值运算符的函数重载已完成，进行了简单的功能测试（已通过）。
当前在进行关于加号运算符的函数重载，在返回值上遇到了问题。使用通过const延长生命周期的临时对象作为返回值时，在运行a=b+c的类似语句会导致程序直接异常终止，原因未知。
或许应该参考一下int的运算？
日志-2025/10/30 10：21
需要注意在math_vector.cpp中关于vector的赋值。因为vector为申请的空间，需要手动释放。因此在赋值前应该主动释放空间，同时需要检查释放为空指针。
日志-2025/10/30 10：43
注意事项：拷贝构造函数不要使用关键词explicit，会导致影响到函数的参数传递与对象的返回。此时会报错找不到对应的构造函数，即使报错提示的函数声明与已声明的构造函数相同也不行。
日志-2025/10/30 21：09
今日完成的关于加减法、赋值的运算重构，同时也补全了类中关于float类型的操作。同时在尝试对点乘进行重构时，发现对于计算完成的返回值是不定的，同时无法对于返回值进行重载。因此开始考虑开始对算式表达式的class类先进行构造。初步的构想的表达式结构式嵌套式的，遍历方式采用树状结构。
日志-2025/10/31 21:35
在草稿图上完成了，关于算式表达式的输入与输出及保存格式的构建。在当前构建的逻辑中，仅支持整数的书写。
日志-2025/11/01 10:57
归纳格式推演的流程，保存格式最初为参考json格式，后简化为数列格式。
    expression:(a-b)+c
    json:{+:{-:a,b},c}
    array:{+,-,a,b,c}
日志-2025/11/07 03:31
确定了当前功能的详细规划。当前制作的表达式仅支持二元函数（运算符）的书写。在完成当前的构建闭环后可以试着，对于多元的运算进行拓展。
已完成expr_set函数的重载，添加了expr_build函数、expr_printf函数的声明与定义，同时完成了上述两个函数的简单调试。之后将输入复制的表达式进行测试。
感觉或许添加的非成员函数可以归类到math_expr类的成员函数中。
日志-2025/11/08 03:03
对函数expr_build进行了运算符优先级运算的测试。并依据测试结果修正了逻辑错误的地方。下一步的测试准备将运算优先级与括号组合进行测试。
重新思考了有何应用。主要是为了可以进行代数运算，以及包含未知量的形式。在后续的编写中operation按照逻辑可以拓展成函数的形式。
日志-2025/11/11 00:47
今日添加了git的本地仓库管理。进一步完成了expr_list_show()与expr_build()的定义与测试。
基本上确定了expr_printf()内部嵌套循环的调用格式，关于同时注意到当前的运算未考虑负数的情况，等待后续添加。
日志-2025/11/11 02:48
完成了expr_printf()函数的定义实现了函数的功能，剩余的优化工作主要为针对括号与运算符优先级的互动。
日志-2025/11/13 15:27
尝试在表达式的结构体中添加深度元素，用以对表达式长度的限制。
日志-2025/11/14 16:13
在添加算式运算逻辑是，发现需求expr_elem需要包含未知量，以完成对左右表达的计算。因此开始在expr_elem中添加label成员。同时此次添加将会记录添加时对各个函数的更改流程。
    expr_elem_type();
    ~expr_elem_type();
    int expr_add_elem(expr_elem &, int , unsigned char , void *)；
日志-2025/11/17 12:34
在添加label成员的途中检查逻辑时发现在expr_add_elem函数中关于oper错误输入的情况未处理。现已添加。
[after line 39]
    expr_set函数相关函数重载
    [before line 43]void expr_elem_free(expr_elem &);
    [before line 43]void expr_list_show(expr_elem &);
    void expr_elem_free(expr_elem &);
日志-2025/11/17 14:53
变量名与函数名区分方式。变量名保存为单独的char[]类型，而函数名称保存为结构体，具体结构待定。
日志-2025/11/17 15:22
[after line 46]
    void expr_printf(expr_elem &);(working)
    发现因为expr_set中做的是浅复制，在释放资源时会二次释放导致进程结束。
日志-2025/11/18 16:09
[result line 43:52]重新编写了expr_set的函数结构，同时在expr_elem作为参数输入时，复制变更为深复制。
[result line 51](done)
日志-2025/11/18 17:00
[after line 51]
    unsigned char expr_build(char *Istr,int len,expr_elem *&expr_head);(working)
    尝试在函数中添加label的逻辑，发生思考变量（label）是否可以包含数字？
        否决！变量应该以字母为主体，若要添加数字标识应该以上下角标的格式加入。同时也为变量的后续拓展方向。
日志-2025/11/18 17:00
[result line 58](done)
突发奇想构建好了expr_build的非嵌套式函数的构建，开始尝试编辑。
日志-2025/12/11 19:28
检查发现expr_set函数对于expr_elem的处理在完成深复制的更改后，没有对源元素进行资源上的释放。
日志-2025/12/12 10:51
[result line 65](done)
在构造expr_build的非嵌套式函数时，发现新的需求。开始构建浅复制类型表达式构建函数expr_link。
日志-2025/12/20 16:21
[result line 68](done)
在编写过程中发现new()不会在空间申请失败时返回空指针（默认情况下，会抛出异常需要通过语句try{}catch{}语句对异常抛出进行捕获与处理）。需要对new()的执行添加std:nothrow参数,以求得其在失败是返回空指针。
日志-2025/12/22 17:40
[result line 63](done)
在测试例子的时候发现嵌套式构建函数出现错误输出，尝试开始重新调试。在完成调试后按计划提交一次版本。
日志-2025/12/23 13:10
在测试例子"(1+(y^2*3)^4-5)/6"时，expr_build的构建仍会出现错误。同时新编写的expr_build_ex表现良好。因此暂时将expr_build的修改停止，进行一次版本提交。
日志-2025/01/01 17:15
expr_build的修改完成，可以完成[line 76]中提及的例子。处理方式是添加了一个find_an_expr函数。由于该函数代码与find_a_right_expr大部分重合，因此在此次版本提交后进行一次复合优化。